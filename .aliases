#config files
if [ -f ~/.servers ]; then
    source ~/.servers
else
    print "Server's not found."
fi

alias confa="nvim ~/.aliases"
alias confi="nvim ~/.config/i3/config"

#system
alias s="sudo"
alias frb="reboot --force"
alias hc="history -c"
alias bye="shutdown -h now"
alias vim="nvim"
#other
alias ydl='youtube-dl -f "bestvideo[height<=1080][ext=mp4]+bestaudio[ext=m4a]" '
